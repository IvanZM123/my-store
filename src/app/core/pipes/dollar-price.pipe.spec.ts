import { DollarPricePipe } from './dollar-price.pipe';

describe('DollarPricePipe', () => {
  it('create an instance', () => {
    const pipe = new DollarPricePipe();
    expect(pipe).toBeTruthy();
  });
});
